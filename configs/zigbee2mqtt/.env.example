# =============================================================================
# DOCKER COMPOSE ENVIRONMENT VARIABLES
# Copy this file to .env and fill in the values
# =============================================================================

# MQTT Broker Credentials (for mqtt-influx-bridge)
# These must match the credentials configured in mosquitto
MQTT_USER=mqtt_user
MQTT_PASSWORD=your_mqtt_password_here

# Home Assistant Long-Lived Access Token (for heater-watchdog)
# Create at: HA → Profile → Long-Lived Access Tokens → Create Token
# Token format: eyJhbG... (JWT token)
HA_TOKEN=your_ha_token_here
