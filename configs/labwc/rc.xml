<?xml version="1.0"?>
<openbox_config xmlns="http://openbox.org/3.4/rc">
  <!-- Touch input configuration -->
  <touch deviceName="ILITEK ILITEK-TP" mapToOutput="HDMI-A-1" mouseEmulation="no"/>

  <!-- Keyboard shortcuts for kiosk control -->
  <keyboard>
    <keybind key="F11">
      <action name="ToggleFullscreen"/>
    </keybind>
  </keyboard>

  <!-- Auto-fullscreen browser windows on startup -->
  <!-- Toggle via kiosk-control API (wlrctl + browser restart approach) -->
  <windowRules>
    <windowRule identifier="org.gnome.Epiphany*">
      <action name="ToggleFullscreen"/>
    </windowRule>
  </windowRules>
</openbox_config>
